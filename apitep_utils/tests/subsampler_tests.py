import unittest

from apitep_utils import Subsampler


class TestSubsampler(unittest.TestCase):

    def test_slice(self):
        subsampler = Subsampler()
        self.assertTrue(False, "This test always fails")
